
  <div class="container-index">
    <h1>All rooms</h1>
    <div class="results">
      {{#each roomsFromDb}}
      <div class="room-tile">
        <div class="thumbnail-parent">
          <img src="{{imageUrl}}" class="thumbnail">
        </div>
        <div class="room-facts">
          <a href="/" class="room-title">{{title}}</a>
          <h2 class="room-rent">{{rent}} €</h2>
          <p class="room-sqm">{{sqr}} sqm</p>
          <div class="period">
          <p>Availible from: {{formatStartDate}} to {{formatEndDate}}</p>
      </div>
        </div>

      </div>
      {{/each}}
    </div>
  </div>
